@model List<ProductViewModel>
@if (Model is not null)
{
    <div class="row">
        @foreach (var item in Model)
        {
            <partial name="/Pages/Shared/Partials/ProductItem.cshtml" model="@item" />
        }
    </div>
}